[package]
name = "sample_organizer"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
walkdir = "2.5"
regex = "1.10"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"